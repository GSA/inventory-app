---
# To apply this manifest: cf push --vars-file vars.yml
applications:
  - name: ((app_name))-solr
    docker:
      image: datagov/catalog-solr:inventory2_8
    routes:
    - route: ((app_name))-solr.apps.internal
  - name: ((app_name))
    buildpacks:
      - https://github.com/cloudfoundry/python-buildpack#v1.7.6
    random-route: true
    # health-check-type: process
    services:
    - ((app_name))-db        # cf create-service aws-rds shared-psql ((app_name))-db
    - ((app_name))-datastore # cf create-service aws-rds medium-psql ((app_name))-datastore
    env:
      DS_RO_USER: datastore_default
      DS_RO_PASSWORD: ((datastore_readonly_password))
      SOLR_URL: http://((app_name))-solr.apps.internal:8983/solr/ckan2_8
